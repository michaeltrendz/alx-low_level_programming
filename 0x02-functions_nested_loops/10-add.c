#include "main.h"
/**
 * add - adds two integers and return the result
 *
 * @c: number being added
 *
 * @d: number being added
 *
 * Return: Always 0.
 *
 */
int add(int c, int d)
{
	return (c + d);
}
